import styled from 'styled-components';

const IngredientsCardContainer = styled.footer`align-items: center;
  bottom: 0;
  display: flex;
  justify-content: space-around;
  position: fixed;
  width: 100%;
`;

export default IngredientsCardContainer;
